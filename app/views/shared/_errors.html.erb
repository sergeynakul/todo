<% if resource&.errors.present? %>
  <div id="error_explanation">
    <h4><%= pluralize(resource.errors.count, "error") %> detected:</h4>
    <ul>
      <% resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>